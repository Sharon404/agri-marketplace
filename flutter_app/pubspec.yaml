name: agri_marketplace_mobile
description: Mobile app for agricultural marketplace
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.3
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.0.5
  intl: ^0.19.0
  url_launcher: ^6.2.6
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true